# Secrets
.dev.vars
.env

# Dependencies
node_modules/
# Lockfiles are committed on purpose: without them every clone re-resolves to whatever
# is newest that day, which is how a wrangler peer-dependency bump broke `npm install`
# for new users. Run `npm update` to move deliberately.

# Build output
client/dist/
dist/

# Wrangler
.wrangler/
wrangler.toml

# OS
.DS_Store
.claude

# Local DB exports
*.sql.backup
prod-backup.sql

# TODO/WIP
TODO.md
*-wip.*